# RegionVTP / QADP 评测命令速查

> 把所有 env 组合收敛成「环境变量总表 + 几行 Recipe」，照着 copy 即可，不再每次手拼。
> 运行环境：gpu10，conda env `agilepruner`，工作目录 `temp/RegionVTP/`（远程路径，本地改动需先同步）。
> 所有评测都压到 **64 token**，脚本第二个参数统一 `64`。
> 数据/脚本准备见 `实验结果.md` 与 `scripts/convert_pope_for_eval.py`。

---

## 〇、完整命令（直接复制）

> 每条都是**完整可执行**的一行，跑在 gpu10 的 `temp/RegionVTP/` 根目录下，不用再拼 env。
> 基准脚本：MME=`mme.sh`、TextVQA=`tvqa.sh`、GQA=`gqa.sh`、POPE=`pope.sh`、SQA=`svqa.sh`。

### ① AgilePruner 原版（默认，ViT 侧 erank 自适应，无需 env）

```bash
METHOD=mme_agilepruner   bash scripts/v1_5/eval/mme.sh  64
METHOD=tvqa_agilepruner  bash scripts/v1_5/eval/tvqa.sh 64
METHOD=gqa_agilepruner   bash scripts/v1_5/eval/gqa.sh  64
METHOD=pope_agilepruner  bash scripts/v1_5/eval/pope.sh 64
METHOD=sqa_agilepruner   bash scripts/v1_5/eval/svqa.sh 64
```

### ② prunesid-pre（内部基线，ViT 侧 PSCA + CLS 注意力）

```bash
SELECTOR=prunesid METHOD=mme_prunesid_pre   bash scripts/v1_5/eval/mme.sh  64
SELECTOR=prunesid METHOD=tvqa_prunesid_pre  bash scripts/v1_5/eval/tvqa.sh 64
SELECTOR=prunesid METHOD=gqa_prunesid_pre   bash scripts/v1_5/eval/gqa.sh  64
SELECTOR=prunesid METHOD=pope_prunesid_pre  bash scripts/v1_5/eval/pope.sh 64
SELECTOR=prunesid METHOD=sqa_prunesid_pre   bash scripts/v1_5/eval/svqa.sh 64
```

### ③ QADP t10（当前最优，LLM 侧 rank-fusion + 多样性抑制）

```bash
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=mme_qadp_t10   bash scripts/v1_5/eval/mme.sh  64
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=tvqa_qadp_t10  bash scripts/v1_5/eval/tvqa.sh 64
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=gqa_qadp_t10   bash scripts/v1_5/eval/gqa.sh  64
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=pope_qadp_t10  bash scripts/v1_5/eval/pope.sh 64
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=sqa_qadp_t10   bash scripts/v1_5/eval/svqa.sh 64
```

### ④ QADP-adaptive ❌（erank 动态阈值，已证负结果，仅存档）

```bash
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_ADAPTIVE=1 METHOD=mme_qadp_adapt bash scripts/v1_5/eval/mme.sh 64
```

> ④ 已证劣于 ③（MME 1675.88 vs 1746.03，`QADP_ERANK_REF=20` 时更差 1654.50），**默认不要跑**，仅留作记录。

---

## 一、当前最优（QADP t10）一条龙

```bash
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 \
QADP_DIVERSITY=1 QADP_TAU=0.10 \
METHOD=mme_qadp_t10 bash scripts/v1_5/eval/mme.sh 64
```

把上面的 `mme.sh` 换成 `tvqa.sh` / `gqa.sh` / `pope.sh` / `svqa.sh`、`METHOD` 换对应名字即可跑全表。

**这条 recipe 的语义**：LLM 第 2 层做 AV+SV rank-fusion 打分，按 fused 分数降序贪心选 64 个，每选一个就抑制掉「层-2 空间余弦距离 < 0.10」的候选（问题自适应多样性），不 merge。

---

## 二、环境变量总表

### 2.1 LLM 侧剪枝（`LLM_LAYER_PRUNE=1` 时生效，TokenCarve / QADP 体系）

| 变量 | 默认 | 含义 |
|---|---|---|
| `LLM_LAYER_PRUNE` | `0` | **总开关**。`1` 才触发 `token_carve_llm_prune`，并让 ViT 放行全部 576 token |
| `TCARVE_LAYER` | `2` | 做 rank-fusion 的 LLM 层（早期层，默认 2） |
| `TCARVE_RANK` | `1.5×N`（=96） | 保留/重选的 token 数。**要 64 必须显式 `=64`** |
| `TCARVE_MERGE` | `N//2`（=32） | merge 数量；`0` = 关掉 merge（select-only） |
| `TCARVE_MODE` | `0` | `0`=AV+SV 融合；`1`=纯 SV；`2`=纯 AV |
| `TCARVE_WEIGHT` | `0.5` | 融合权重 λ（AV 权重，SV 为 1−λ） |
| `QADP_DIVERSITY` | `0` | `1`= 用贪心 + 余弦距离阈值选（QADP 多样性项） |
| `QADP_TAU` | `0.2` | QADP 静态相似度阈值（当前最优 0.10），`QADP_ADAPTIVE=1` 时被忽略 |
| `QADP_ADAPTIVE` | `0` | `1`= 用 erank 自适应阈值 τ_i=order_i×(erank/ref×0.01)（AgilePruner Eq.6），替代静态 τ |
| `QADP_ERANK_REF` | `90` | 自适应阈值的 erank 参考值（AgilePruner 的 ViT 标定值；LLM 空间 erank≈13~21，按 ~20 重标已试仍差于静态） |
| `QADP_TAU_MAX` | `0.25` | 自适应阈值上限 |

### 2.2 ViT 侧选择（`LLM_LAYER_PRUNE` 未设/为 0 时生效，AgilePruner / PRUNESID 体系）

| 变量 | 默认 | 含义 |
|---|---|---|
| `SELECTOR` | `agilepruner` | `prunesid`=PSCA-NMS 选择器；否则 AgilePruner 贪心 |
| `DIST_THRESHOLD` | 无 | 给 AgilePruner 指定静态 τ（覆盖 erank 自适应，做 τ 消融用） |
| `QUERY_LAMBDA` | `1.0` | `<1.0` 时把 CLS 注意力与「CLIP 问题相似度」混合排序 |
| `PRUNESID_QUERY` | `0` | `1` + `SELECTOR=prunesid` 时开启问题加权 Q-PSCA |
| `QPSPCA_TEMP` | `0.5` | Q-PSCA 的 softmax 温度 |
| `PRUNESID_SPACE` | `pre` | `pre`=ViT 倒数第二层(1024)；`post`=mm_projector 后(4096) |
| `REGION_SIZE` | `0` | `>1` 时走 region-wise 选择器（需 `side×side==N` 且 `side%R==0`） |
| `REGION_GAMMA` | `1.0` | region 选择器权重 |
| `BUDGET_MODE` | `waterfill` | region 选择器预算分配 |
| `MERGE_MODE` | `off` | `nearest`=ViT 侧 ToMe 式 merge（阶段 1 实测负贡献） |
| `CLIP_TEXT_MODEL` | 自动 | 问题文本编码用的 CLIP 模型 |

### 2.3 通用

| 变量 | 默认 | 含义 |
|---|---|---|
| `METHOD` | `regionvtp` | 结果存放目录名，建议每次一个独立名字 |
| `CUDA_VISIBLE_DEVICES` | `0` | 多卡时 gqa/vqav2 会按逗号分 chunk |

---

## 三、Recipe 速查

> 每条是一组完整的 env 前缀，直接拼在 `bash scripts/v1_5/eval/<bench>.sh 64` 前面用。

### 3.1 QADP（当前候选）

| 名称 | env 组合 |
|---|---|
| **QADP t10（最优）** | `LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10` |
| QADP τ 扫描 | 同上，`QADP_TAU` 换成 `0.20 / 0.30 / 0.50` |
| **QADP-adaptive（erank 动态阈值）** ❌ 已测负结果 | `LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_ADAPTIVE=1`（MME 1675.88 / ref=20 时 1654.50，均差于 t10 1746.03） |
| QADP-adaptive 调参 | 同上，再改 `QADP_ERANK_REF` / `QADP_TAU_MAX`（已证 ref=90/20 都不如静态 0.10） |

> 自适应阈值行为（LLM 空间 erank≈20 时）：`τ_i = order_i × (20/90 × 0.01) = order_i × 0.0022`，上限 0.25。
> 即排名前 ~45 的 token 阈值比静态 0.10 **更小**（头部更密集、几乎不抑制），排名 45 之后阈值**更大**（尾部抑制更狠）。这跟静态 0.10 是两种不同的选择行为，**已 A/B：静态 0.10 明显更优**（阶段 5）。

### 3.2 TokenCarve 移植（阶段 2/3，rank-fusion 体系）

| 名称 | env 组合 |
|---|---|
| rank-fusion + merge | `LLM_LAYER_PRUNE=1`（默认 rank=96、merge=32 → 64） |
| rank-fusion 无 merge | `LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0` |
| AV-only | `LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 TCARVE_MODE=2` |
| SV-only | `LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 TCARVE_MODE=1` |
| layer 扫描 | 在「无 merge」基础上加 `TCARVE_LAYER=<2/4/8/...>` |
| 融合权重扫描 | 在「无 merge」基础上改 `TCARVE_WEIGHT=<0~1>` |

### 3.3 ViT 侧（阶段 1，已饱和，仅作对照基线）

| 名称 | env 组合 |
|---|---|
| prunesid-pre（内部基线） | `SELECTOR=prunesid` |
| prunesid + merge | `SELECTOR=prunesid MERGE_MODE=nearest` |
| Q-PSCA | `SELECTOR=prunesid PRUNESID_QUERY=1` |
| AgilePruner 静态 τ 消融 | `DIST_THRESHOLD=<τ>`（默认 SELECTOR=agilepruner） |

---

## 四、benchmark 命令模板

统一模板（gpu10 上 RegionVTP 根目录）：

```bash
<RECIPE_ENV> METHOD=<run_name> bash scripts/v1_5/eval/<bench>.sh 64
```

| 基准 | 脚本 | 主指标 | 输出位置 |
|---|---|---|---|
| MME | `mme.sh` | 总分（P/C） | `playground/data/eval/MME/answers/...` |
| TextVQA | `tvqa.sh` | Accuracy | `playground/data/eval/textvqa/answers/...` |
| GQA | `gqa.sh` | Accuracy | `playground/data/eval/gqa/answers/...` |
| POPE | `pope.sh` | Average F1 | `playground/data/eval/pope/answers/...` |
| SQA | `svqa.sh` | Accuracy | `playground/data/eval/scienceqa/answers/...` |

**已修脚本**（`METHOD` 可变 + 本地模型路径 `/groups/g900403/home/share/phr/models/liuhaotian/llava-v1.5-7b`）：`mme.sh / tvqa.sh / gqa.sh / pope.sh / svqa.sh`。
**未修脚本**（仍是 `METHOD=agilepruner` 硬编码 + HF hub 路径 `liuhaotian/${CKPT}`）：`mmbench.sh / mmbench_cn.sh / vizwiz.sh / vqav2.sh`——用前需先改成和上面 5 个一致。

完整一套（QADP t10 全表，可直接顺序跑）：

```bash
export QADP='LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10'

$QADP METHOD=mme_qadp_t10  bash scripts/v1_5/eval/mme.sh  64
$QADP METHOD=tvqa_qadp_t10 bash scripts/v1_5/eval/tvqa.sh 64
$QADP METHOD=gqa_qadp_t10  bash scripts/v1_5/eval/gqa.sh  64
$QADP METHOD=pope_qadp_t10 bash scripts/v1_5/eval/pope.sh 64
$QADP METHOD=sqa_qadp_t10  bash scripts/v1_5/eval/svqa.sh 64
```

> 注意：`export QADP='...'` 里带空格作为前缀展开是可行的（`$QADP cmd`），但更稳妥是直接写 inline。上面仅为演示。

---

## 五、易错点

1. **`LLM_LAYER_PRUNE=1` 是 QADP/TokenCarve 的必需开关**。漏了它，模型走的是 ViT 侧 AgilePruner 剪枝，QADP 完全不生效（`实验结果.md` 里那句「固定命令」就漏了它）。
2. **`TCARVE_RANK` 默认是 1.5×N=96，不是 64**。想要「纯 64 token 的 select-only」，必须同时 `TCARVE_RANK=64 TCARVE_MERGE=0`；只写 `TCARVE_MERGE=0` 会得到 96 token，和 64 的对比不公平。
3. **`TCARVE_MERGE` 默认 32**。TokenCarve 原始语义是「先选 96 再 merge 回 64」；QADP 走 select-only，必须显式 `=0`。
4. **QADP 阈值有两种模式**：静态（`QADP_TAU=0.10`，当前最优）与 erank 自适应（`QADP_ADAPTIVE=1`，已测负结果）。自适应模式沿用 AgilePruner 的 ViT 标定值 `QADP_ERANK_REF=90 / QADP_TAU_MAX=0.25`，但 LLM 空间 erank 只有 ~13~21，`erank/90≈0.15~0.23` 会让 τ 整体偏小、头部几乎不抑制——于是按 LLM 空间重标（`QADP_ERANK_REF=20`）也试了，两者 MME 均差于静态 0.10（−70 ~ −92），这条路已放弃。`TCARVE_RANK/QADP_DIVERSITY` 之外的其他 LLM 侧参数（layer/weight/mode）目前沿用默认，未重新扫。
5. **脚本要跑在 gpu10 上**，本地 Windows 只是编辑端；改完代码要同步到 gpu10 再跑。
