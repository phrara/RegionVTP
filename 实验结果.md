# RegionVTP / QADP 实验记录

> 训练免费的视觉 token 剪枝（fork 自 AgilePruner, ICLR 2026），目标是提出一个与 AgilePruner / PRUNESID / TokenCarve 同档甚至略优的新算法。
> 模型固定 **LLaVA-1.5-7B**，压缩目标 **64 token**（576 → 64，保留 11.1%）。
> 评测在远程 GPU 服务器 `gpu10`（conda 环境 `agilepruner`）上跑，greedy 解码，temperature=0。
> 更新日期：2026-08-24。

---

## 〇、基准参考值

### 全量 & 论文报告值（LLaVA-1.5-7B）

| 来源 | MME | TextVQA | GQA | POPE | SQA |
|---|---|---|---|---|---|
| Vanilla 全量（576 token） | 1862 | 58.2 | 61.9 | 85.9 | 69.5 |
| AgilePruner @64（论文） | 1703 | 56.0 | 57.4 | 84.1 | 68.6 |
| PRUNESID @64（论文） | **1733** | 未报告 | 57.1 | 83.8 | 67.8 |

### 内部基线（本机复现）

| 基线 | MME 总分 (P/C) | TextVQA | 说明 |
|---|---|---|---|
| **prunesid-pre** | **1706.07** (1398.93 / 307.14) | **55.50** | `SELECTOR=prunesid`，PRUNESID 风格 ViT 侧选择，后续所有比较的相对锚点 |

> 说明：`prunesid-pre` 是我们实现的 PRUNESID 风格选择（PSCA 分组 + CLS 注意力打分），MME 1706 落在 AgilePruner 1703 与 PRUNESID 论文 1733 之间——它只复现了选择机制本身，未包含 PRUNESID 的全部组件，故与论文 1733 有差距。另外 AgilePruner 官方脚本复现的 TextVQA 为 56.04（见 `mllm/vi-token-reduction/论文复现结果记录.md`）。

---

## 一、分阶段实验

### 阶段 1：ViT 侧选择已饱和（结论：在 ViT 输出层继续优化无效）

在 ViT 输出层试了三类改进——merge（ToMe 式合并）、query 条件化（λ-blend + Q-PSCA）、region 自适应预算——全部 ≤ 基线。

**MME（完整变体表）**：

| 配置 | P | C | 总分 | erank |
|---|---|---|---|---|
| **prunesid-pre（基线）** | **1398.93** | 307.14 | **1706.07** | 19.57 |
| AgilePruner 原版（`mme_base`，默认 selector） | 1396.19 | 301.79 | 1697.98 | 19.24 |
| AgilePruner + merge（`MERGE_MODE=nearest`） | 1392.35 | 303.21 | 1695.56 | 18.34 |
| prunesid + merge（`MERGE_MODE=nearest`） | 1368.70 | 287.5 | 1656.20 | 20.13 |
| Q-PSCA（`PRUNESID_QUERY=1`，CLIP 文本加权 PCA） | 1381.32 | 294.64 | 1675.96 | 19.56 |
| **region R=4（`REGION_SIZE=4`）** | 1360.62 | **308.93** | **1669.55** | **13.00** |

**TextVQA（query λ 扫描，`QUERY_LAMBDA`，λ 越小问题权重越大）**：

| λ | TextVQA | 说明 |
|---|---|---|
| 1.0（=原版，纯 CLS 注意力） | **56.04** | 基线 |
| 0.7 | 55.61 | 略降 |
| 0.5 | 55.57 | 略降 |
| 0.3 | 53.40 | 明显降 |
| 0.0（纯 query，不掺注意力） | 48.11 | 崩（erank 30.4，几乎随机） |
| 0.5 + region R=4（全开） | 53.55 | query + region 叠加更差 |

**结论**：
- **region 负贡献**：R=4 → 1669.55（erank 13.00），比两个基线都低（−28 vs AgilePruner 1698、−36 vs prunesid-pre 1706）。region 在自己的设计目标（缩小 MME 与 PRUNESID 的差距）上直接判死。注意它的 cognition 是全表最高（308.93，floor=1 保证空间覆盖 → 利好全局题），但 perception 崩到 1360.62（预算被摊薄 / 复杂度加权错配）。
- **query 负贡献**：λ 越小越差，λ=0 纯 query 崩到 48%（erank 30.4，几乎随机）。Q-PSCA 也 −30：问题加权把 PCA 主成分朝少数显著 patch 旋转，等于删掉 prunesid-pre 靠多样性保的细粒度能力——净 −30（涨的「语义身份」类 +34、跌的「OCR/code/color/position」类 −64，同一次旋转的两面）。**ViT 层的问题条件化是死路**：CLIP/embed 文本对 patch 的余弦与 CLS 注意力高度相关（都指向「哪个 object 显著」），跨模态权重只添乱。
- **merge 负贡献**：两种 merge（叠在 AgilePruner 上、叠在 prunesid 上）都掉分。
- 综上：**ViT 侧的「选哪些 token」信号已饱和**。问题条件化必须到 LLM 层做（LLM attention 天然问题条件），这就是转向 TokenCarve rank-fusion 的动机。

### 阶段 2：TokenCarve 移植（LLM 层 rank-fusion，`LLM_LAYER_PRUNE=1`）

在 LLM 前 2 层做 partial forward，用问题注意力（AV）+ SVD 行贡献（SV）rank-fusion 重选图像 token。

| 配置 | P | C | 总分 | erank |
|---|---|---|---|---|
| rank-fusion + merge（默认） | 1365.41 | 320.71 | 1686.12 | 13.09 |
| **rank-fusion 无 merge**（TCARVE_MERGE=0） | **1399.80** | **326.07** | **1725.87** | 13.10 |
| AV-only（TCARVE_MODE=2，纯问题注意力） | 1227.63 | 323.93 | 1551.56 | 10.08 |

**TextVQA**：rank-fusion 无 merge = **54.88**（vs prunesid-pre 55.50，**−0.62**）。

**结论**：
- rank-fusion 无 merge 的 MME **1725.87**，首次超过 prunesid-pre（+20），是当时最优。
- 但 TextVQA **掉分**（55.50 → 54.88），与「MME 翻译项涨」不同步。诊断：rank-fusion 的 top-k **过度聚焦**，预算全砸在答案相关区域，读图覆盖不足。
- AV/SV 分解：**AV（问题注意力）→ 认知/翻译**（纯 AV 认知仍 323.9）；**SV（SVD 覆盖）→ 感知**（纯 AV 感知崩到 1227.6）。merge 恒为负贡献（3 次确认）。

### 阶段 3：layer 扫描（`TCARVE_LAYER`）

| 配置 | P | C | 总分 | erank |
|---|---|---|---|---|
| layer=2（rank-fusion 无 merge） | 1399.80 | 326.07 | **1725.87** | 13.10 |
| layer=8 | 1325.97 | 308.57 | 1634.54 | 13.30 |

**结论**：深层假设被否决。layer=8 感知、认知双崩（−91）。早期注意力（layer 2）才是干净的显著性信号，与 TokenCarve/FastV 选 layer 2 一致——这个 ablation 反过来为 layer 选择背书。

### 阶段 4：QADP 多样性覆盖项（`QADP_DIVERSITY=1`，本会话新增）

修复过度聚焦：把 `topk(fused, rank)` 换成 AgilePruner 的贪心选择器——按 rank-fusion 相关度排序，但每选一个 token 就抑制掉 LLM 问题空间里余弦距离 < `QADP_TAU` 的候选，逼预算铺开。距离在 layer-2 上下文特征上算（区别于 AgilePruner 的 ViT 空间），即「问题自适应多样性」。

**MME 扫 tau**：

| QADP_TAU | P | C | 总分 | erank |
|---|---|---|---|---|
| **0.10** | **1415.67** | 330.36 | **1746.03** ✅ | **20.42** |
| 0.20 | 1379.37 | 288.57 | 1667.94 | 21.27 |
| 0.30 | 1369.56 | 303.93 | 1673.49 | 16.21 |
| 0.50 | 1378.72 | **340.36** | 1719.08 | 13.70 |
| （对照）rank-fusion 无 merge | 1399.80 | 326.07 | 1725.87 | 13.10 |

**TextVQA @ tau=0.10**：**55.34**（erank 21.44），vs prunesid-pre 55.50（−0.16）、rank-fusion 54.88（+0.46）。

**结论**：
- **多样性项生效（硬信号）**：erank 13.10 → 20.42，选择不再冗余。
- **tau=0.10 是当前最优**：MME **1746.03**（+20 vs rank-fusion、+40 vs prunesid-pre），TextVQA 55.34（找回 rank-fusion 掉分的 74%，但距 prunesid-pre 还差 −0.16 ≈ 8 样本）。
- **erank 与 tau 非单调**：tau 过大（0.3/0.5）时贪心一上来抑制掉大片区域，候选池打空后走「纯注意力回填」分支，又把冗余 token 塞回来（erank 掉回 13.7~16.2）。多样性存在很窄的最优窗（≈0.1），自适应阈值必须按 LLM 空间重新标定，不能套 AgilePruner 的 ViT 空间参考值。

---

### 阶段 5：erank 自适应阈值（负结果，`QADP_ADAPTIVE=1`）

把静态 `QADP_TAU` 换成 AgilePruner 式 erank 自适应阈值 `τ_i = order_i × (erank/ref × 0.01)`（cap 0.25），在 LLM 空间跑：

| 配置 | P | C | 总分 | erank | Δ vs t10 |
|---|---|---|---|---|---|
| **QADP t10（静态 0.10）** | **1415.67** | **330.36** | **1746.03** | **20.42** | — |
| adaptive ref=90（Agile 原值） | 1363.74 | 312.14 | 1675.88 | 20.44 | −70.15 |
| adaptive ref=20（LLM 重标定） | 1347.72 | 306.79 | 1654.50 | 19.60 | −91.53 |

**结论**：erank 自适应阈值**明显更差**（−70 ~ −92），静态 0.10 保持最优，这条路走不通。感知子项看：ref=90 的 existence 190（+15 vs t10 的 175）但 count/position/color/artwork 全线掉——头部小 τ 让选择更密集、利于「有没有」这类存在性判断，但尾部大 τ 过度抑制、伤了需要全图覆盖的细粒度项。三个配置 erank 几乎不变（19.6~20.4）却差出 90 分，再次印证「erank 是混淆代理、不预测质量」。

---

## 二、当前最优与最终结论

**QADP tau=0.10 是候选算法**，综合「一胜两平一负」（vs prunesid-pre）。

### 2.1 主对照表（64 token，同一 harness 本机复现）

| Benchmark | AgilePruner 复现 | prunesid-pre | QADP t10 | QADP−prunesid | QADP−Agile |
|---|---|---|---|---|---|
| MME 总分 | 1698 | 1706.07 | **1746.03** | **+40** ✅ | +48 |
| TextVQA | 56.04 | 55.50 | 55.34 | −0.16 ⚖️ | −0.70 |
| POPE F1 | 84.65 | 84.50 | 82.70 | **−1.80** ❌ | −1.95 |
| SQA^IMG | 68.96 | 68.47 | 68.47 | 0.00 ⚖️ | −0.49 |
| GQA | 待跑 | 待跑 | 待跑 | — | — |

> 主指标：MME=总分(P/C)、TextVQA/GQA/SQA=Accuracy、POPE=Average F1；SQA 用 IMG 子集（全量 4241 题 ~52% 纯文本，会稀释视觉剪枝差异）。AgilePruner 原版 MME 复现 = **1698**（`mme_base`，见阶段 1），略低于论文 1703（eval 方差内）。GQA 未跑（数据/脚本待装，见「下一步」）。

### 2.2 对论文参考值

论文值（AgilePruner @64：MME 1703 / TextVQA 56.0 / GQA 57.4 / POPE 84.1 / SQA 68.6）：QADP 相对 MME **+43**、TextVQA −0.66、POPE −1.40、SQA −0.13。MME 领先幅度远大于其余落后项；POPE 是唯一稳定的负向。

### 2.3 关键观察

- **POPE 负向是真实的**：两个基线本机复现（84.65 / 84.50）都略高于论文值（84.1 / 83.8），QADP 却更低，排除 harness 问题。机制同阶段 4：多样性抑制更保守（yes ratio 更低）+ recall 更低（0.739 vs 0.776），漏检「图里是否有某物」的细粒度对象验证。
- **SQA / TextVQA 打平**：QADP 与 prunesid-pre 在 SQA 完全一致（correct 都 2957），TextVQA 差 8 样本（−0.16）。粗粒度多选任务里 QADP 与基线等价。
- **MME 大胜**：+40 全部来自认知/翻译项，是 QADP「问题注意力 + 多样性铺开」的主要卖点。

### 2.4 附带数据点

AgilePruner 原版跑 POPE 打印 `Average erank of selected tokens: 20.33`——即「Agile 在 ViT 空间 erank」本机实测 ≈ 20.3（post-mm_projector 4096 维）。巧合的是 QADP t10 在 LLM 层-2 空间的 erank 也是 20.42，但两者不同空间、不可直接比较。

**核心洞察（erank 是混淆代理）**：低 erank（~13）既可能是坏配置（region R=4 → 1670），也可能是最优（rank-fusion → 1725.9）。关键不是「多样性高低」，而是「选对了 token」；多样性只在相关度排序正确的前提下，用来补覆盖。

---

## 三、下一步：补 GQA

5 项 benchmark 里只剩 **GQA** 未跑（MME / TextVQA / POPE / SQA 已齐，三方法对照见 2.1 主表）。

| Benchmark | 脚本 | 状态 |
|---|---|---|
| GQA | `gqa.sh` | ⏳ 数据/脚本待装：下官方 questions+images、改 `eval.py`（LLaVA gist）、生成 `llava_gqa_testdev_balanced.jsonl` |
| POPE | `pope.sh` | ✅ 82.70 / 84.50 / 84.65 |
| SQA | `svqa.sh` | ✅ 68.47 / 68.47 / 68.96（IMG-Acc） |

GQA 三方法命令（装好数据后，注意 METHOD 名）：

```bash
METHOD=gqa_agilepruner   bash scripts/v1_5/eval/gqa.sh 64
SELECTOR=prunesid METHOD=gqa_prunesid_pre bash scripts/v1_5/eval/gqa.sh 64
LLM_LAYER_PRUNE=1 TCARVE_RANK=64 TCARVE_MERGE=0 QADP_DIVERSITY=1 QADP_TAU=0.10 METHOD=gqa_qadp_t10 bash scripts/v1_5/eval/gqa.sh 64
```

> **搁置项**：`temp/popev2/` 自定义 POPE 数据存在 label 冲突（同一张图同一问题两条相反 label：`/coco/train2017/`→Yes、`/images/`→No，500 对），用户暂未确定其含义，先不转换。
