# 本周工作总结 - 向流式场景迁移

> 汇报日期：2026-08-31
> 本周主题：调研流式场景的视觉 token 压缩，精读 STC 论文与代码，评估其融入我们现有方案的可行性。

---


1. **流式场景的视觉 token 压缩**，在候选方法中选定 STC 精读，弄懂了它的原理。
2. **梳理 STC 的论文主要实验结果**，理解它「又快又不掉精度」的关键在哪。
3. **分析把 STC 融入我们方案的可行性**，给出分阶段的落地思路。


---

## 一、主要问题

1. 哪些 token 需要重复计算
2. 哪些 token 需要保留


## 二、STC 原理

STC 全称 *Streaming Token Compression*（流式 token 压缩），核心思路是**分层压缩**：在模型的两个不同位置各放一个组件，分别解决「帧间重复」和「帧内太多」两个问题。

```
一段视频流
   ↓
【STC-Cacher】在 ViT 内部：复用静止 token，只重算变化的 token   → 解决帧间重复
   ↓
【STC-Pruner】在 ViT 之后、LLM 之前：每帧只留最显著的 token      → 解决帧内太多
   ↓
LLM 读入压缩后的 token，回答问题
```

### 2.1 组件一：STC-Cacher（在 ViT 内部做「选择性重算」）

**放在哪个模块**：ViT（视觉编码器）的每一层内部。ViT 有 20 多层，STC 直接「接管」了每一层的计算方式。

**观察到的事实**：相邻两帧画面高度相似，大部分 patch（小方块）的特征和上一帧几乎没变。如果每帧都从头把 20 多层完整算一遍，等于把 75% 没变的内容重复算了一次。

**提取了什么信息**：它把某一帧完整算一遍，作为「参考帧」，并把每一层算到一半的**中间结果**（key 特征、value 特征、注意力输出、MLP 输出）都存下来。之后到来的新帧，先拿「当前帧每个 patch 的 key 特征」和「参考帧对应的 key 特征」比**余弦相似度**——相似度越低，说明这个 patch 变化越大。

**干了什么操作**（三步）：

1. **挑**：选出和参考帧**最不相似的 25%** patch（默认 `update_token_ratio=0.25`），这些是「真正变了」的区域（比如动了的人、新出现的物体）。
2. **只重算这 25%**：对选出的 patch 重新做完整的注意力 + 特征加工。
3. **其余 75% 直接抄**：没变的部分，直接复用参考帧已经存好的中间结果，不再重算。

另外每隔若干帧（`cache_interval`，默认 4 帧）会完整重算一次，刷新参考帧，避免误差累积。形象地说：**背景墙不动就不重新画，只在动了的位置上更新画面**。

> 一个技术彩蛋：这种「只算一小部分」的操作，在高速 GPU 上反而可能因为「启动开销」变慢（小操作太多，调度本身比计算还贵）。STC 用 CUDA graph（把一连串小操作打包成一次回放）解决了这个问题——这是它工程上最扎实的地方。

### 2.2 组件二：STC-Pruner（在 LLM 之前做「显著性剪枝」）

**放在哪个模块**：ViT 处理完之后、送入 LLM 之前。这时视觉特征已经「加工好了」，是决定「喂给 LLM 多少 token」的最后一道关口。

**观察到的事实**：LLM 读 token 的开销和 token 数量成正比。视频每帧 196 个 token，几十帧就是几千上万，LLM 光「读」就慢得离谱。而其中大量 token 是冗余的（空白背景、重复纹理），对回答问题没帮助。

**提取了什么信息**：它维护两个「锚点」（可以理解为两个参照标准）——
- **空间锚**：当前这一帧所有 patch 的**平均值**；
- **时间锚**：**历史若干帧**的平均值（一个很轻的记忆，只存「平均长相」）。

然后算每个 patch 特征和这两个锚点的**差异程度**：差异越大 = 这个 patch 越「与众不同」= 越有信息量（novelty）。

**干了什么操作**：

1. **打分**：每个 patch 按「和空间锚、时间锚的差异」加权（`dual_anchor` 策略），得到一个「新颖度 / 显著性」分数。
2. **排序留前 K**：每帧只保留分数最高的 64 个 token（默认 `token_per_frame=64`），其余丢弃。
3. （可选的一步）还会先做一次**通道剪枝**：把 50% 波动最小的特征通道去掉（token 是「图词的数量」，通道是「每个词的信息维度」，是两个正交的压缩轴）。

形象地说：**只把画面里最「抓眼球」的部分留给 LLM，平淡的背景直接丢掉**。

### 2.3 两个组件一句话总结

| 组件 | 位置 | 剪什么 | 通俗说法 |
|---|---|---|---|
| STC-Cacher | ViT 内部 | 帧间重复 | 背景不动就不重算，只更新变化的部分 |
| STC-Pruner | LLM 之前 | 帧内冗余 | 只把最显著的部分喂给 LLM，其余丢弃 |

---

## 三、实验结果

### 3.1 主结果：流式 benchmark（ReKV 框架上）

对比对象是三个已有的 token 压缩方法（ToMe、VisionZip、VidCom²），指标是四个视频理解任务（OVO 实时 / 回看 / 前瞻、StreamingBench）的准确率 + 两个延迟。

| 方法 | OVO 实时 | OVO 回看 | OVO 前瞻 | StreamingBench | ViT 编码延迟 | LLM prefill 延迟 |
|---|---|---|---|---|---|---|
| ReKV（不压缩基线） | 64.4 | 64.6 | 52.6 | 69.1 | 103.7 | 482.4 |
| + ToMe | 53.1 | 60.7 | 46.4 | 59.4 | 70.5（↓32%） | 257.8（↓46.6%） |
| + VisionZip | 53.8 | 58.4 | 47.5 | 60.4 | 103.7 | 258.3（↓46.5%） |
| + VidCom² | 60.4 | 59.0 | 50.4 | 63.6 | 103.7 | 259.1（↓46.3%） |
| **+ STC（Cacher+Pruner）** | **62.4** | **62.9** | **52.1** | **65.0** | **78.4（↓24.5%）** | **263.7（↓45.3%）** |



- 前三个方法虽然也把延迟降下来了（LLM prefill 都降了 ~46%），但**准确率掉得厉害**（ToMe 从 64.4 掉到 53.1，掉了 11 分）。
- **只有 STC 做到了「快」和「准」兼得**：准确率几乎没掉（64.4→62.5，仅 −1.9），同时 ViT 编码快 24.5%、LLM prefill 快 45.3%。
- 特别值得注意的是 **VisionZip 和 VidCom² 的 ViT 编码延迟完全没降（103.7 不变）**，因为它们的压缩都在 ViT 之外做，动不了「看图」这道工序的开销；而 STC-Cacher 深入 ViT 内部，连「看图」这一步本身都省了 24.5%——这是它区别于所有已有方法的核心优势。

### 3.2 STC-Cacher 的通用性（换到别的视频模型上依然有效）

把 STC-Cacher 单独挂到另外三个流式视频模型上（只做帧间复用、不做剪枝），看它是否「即插即用」：

| 框架 | OVO 实时 | OVO 回看 | OVO 前瞻 | ViT 编码延迟 |
|---|---|---|---|---|
| Dispider | 51.0 → 49.1 | 40.1 → 36.6 | 40.4 → 39.2 | 26.4 → 18.9（↓28.4%） |
| LiveCC | 57.0 → 53.8 | 56.4 → 54.2 | 59.7 → 57.3 | 181.2 → 126.8（↓30.0%） |
| StreamForest | 61.6 → 59.1 | 70.8 → 68.2 | 54.3 → 52.3 | 103.7 → 67.7（↓34.7%） |

**解读**：换到三个完全不同的模型上，ViT 编码延迟稳定下降 28%~35%，准确率只小幅下降（−1.8 ~ −2.5）。说明 STC-Cacher 是**与具体模型无关的通用加速件**，这是「即插即用」的直接证据。

### 3.3 离线长视频理解（STC-Pruner 单独的效果）

在三个离线长视频理解数据集上（EgoSchema / MLVU / VideoMME），只开 STC-Pruner：

| 方法 | EgoSchema | MLVU | VideoMME | 平均 |
|---|---|---|---|---|
| ReKV（基线） | 57.7 | 68.6 | 57.7 | 61.3 |
| + ToMe | 55.2 | 63.1 | 51.7 | 56.7 |
| + VisionZip | 55.8 | 63.2 | 51.6 | 56.9 |
| + VidCom² | 60.6 | 67.1 | 56.8 | 61.5 |
| **+ STC-Pruner** | **60.8** | **67.6** | **57.1** | **61.8** |

**解读**：STC-Pruner 在离线长视频上**不仅不掉精度，平均分还略高于基线**（61.8 vs 61.3），而 ToMe/VisionZip 掉了 4~5 分。说明它「留最显著 token」的选择策略本身是高质量的，剪掉的是真冗余，不是信息。

---

## 四、Ours 向流式场景迁移


### 重新对齐

| 维度 | 我们（QADP）转向流式后 | STC |
|---|---|---|
| 处理对象 | 视频流（多帧连续） | 视频流（多帧连续） |
| 帧内剪枝 | ✅ 已有（问题条件化 rank-fusion + 多样性） | STC-Pruner（纯视觉锚点打分） |
| 帧间复用 | ❌ 没有 | ✅ STC-Cacher（选择性重算） |
| 选择依据 | **问题注意力**（对问题感知） | 纯视觉（对问题无感知❌） |
| 免训练 | 是 | 是 |

**一句话结论**：STC 有「帧间复用」、我们缺；我们有「问题条件化」、STC 缺。两者合起来正好是一套完整的流式方案——**STC 解决「快」，我们解决「准」**。



### 4.4 转向流式需要解决的新问题（之前没碰过的）

1. **模型底座迁移（最大的决策）**：我们现在是 LLaVA-1.5-7B（单图、CLIP、576 token/图）。要做流式视频，要么换到视频原生底座（LLaVA-OneVision / LLaVA-Video，即 STC 的 ReKV 底座），要么给 LLaVA-1.5 加多帧包装。**这比算法本身更影响工作量，建议优先评估换底座的成本**。
2. **预算从「每图 64」变成「每帧 64 × N 帧」**：压缩比的定义，以及「问题注意力要在 LLM 前 2 层做 partial forward」的成本，在多帧下要重新核算（partial forward 可摊到整段视频，未必是 N 倍）。
3. **跨帧多样性**：我们现有的多样性项（余弦距离 < τ 抑制）是帧内的；流式下要不要扩展到「跨帧抑制」（避免相邻帧重复选同一位置）是新的消融维度。

### 4.5 重新给出的融合方案（三阶段，比之前更直接）

**阶段一（复现 + 对齐底座）**：在 ReKV（或换好的视频底座）上复现 STC 的延迟与准确率，作为新基线；同时解决「模型底座迁移」问题。

**阶段二（帧内替换，我们的核心贡献）**：把 STC-Pruner 的「纯视觉锚点打分」换成我们的「问题注意力 + 多样性」rank-fusion，帧间继续用 STC-Cacher。目标：相同 token 预算下，把准确率损失从 STC 的 −1.9 进一步压到接近 0。

**阶段三（跨帧多样性 + 完整消融）**：把 QADP 的多样性抑制从帧内扩展到跨帧，做完整消融表，形成「问题感知 + 时序复用 + 跨帧去冗余」的完整流式方案（暂定名 **QADP-streaming**）。

### 4.6 本阶段的结论

- 前提从「静态图」转向「流式视频」后，STC 的角色从「借鉴对象」升级为「**可以直接站立其上的骨架**」：帧间复用直接复用，帧内剪枝用我们的问题条件化替换。
- 我们的差异化定位清晰：**STC 解决「快」，我们解决「准」——把「问题感知」注入流式剪枝，是 STC 论文明确没做、且流式问答里真实需要的方向**。
- 最大风险不在算法，而在**模型底座迁移 + 视频数据/评测环境**的成本，建议把「复现 STC（换底座）」作为第一步先落地。

